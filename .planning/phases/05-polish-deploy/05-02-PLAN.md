---
phase: 05-polish-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - .env.example
  - src/app/layout.tsx
  - src/app/api/chat/route.ts
  - next.config.ts
autonomous: false
requirements: [DEPL-01, DEPL-02, DEPL-03, DEPL-04, DEPL-05]

must_haves:
  truths:
    - "Site is accessible at goodai.au via Vercel"
    - "AI_GATEWAY_API_KEY is only in server-side env — never in NEXT_PUBLIC_ prefix, never in client bundle"
    - "Page loads under 2 seconds on simulated 4G throttling"
    - "Browser console shows zero errors on production URL"
    - "Lighthouse performance score is 90 or above"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: "goodai.au"
    - path: ".env.example"
      provides: "Environment variable template documenting required vars"
      contains: "AI_GATEWAY_API_KEY"
    - path: "src/app/layout.tsx"
      provides: "Production metadata, viewport config, font preloading"
      contains: "viewport"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "process.env.AI_GATEWAY_API_KEY"
      via: "server-side env access (no NEXT_PUBLIC_ prefix)"
      pattern: "process\\.env\\.AI_GATEWAY_API_KEY"
    - from: "vercel.json"
      to: "Vercel deployment"
      via: "project config with headers and rewrites"
      pattern: "headers"
    - from: "src/app/layout.tsx"
      to: "performance"
      via: "metadata viewport, font preload, no CLS"
      pattern: "viewport"
---

<objective>
Deploy the production site to goodai.au on Vercel, ensure env var security, and hit performance targets (Lighthouse 90+, sub-2s load on 4G).

Purpose: The site must be live on the production domain with zero console errors and fast load times to convert Perth SME visitors.
Output: Live site at goodai.au, passing all deployment requirements.
</objective>

<execution_context>
@C:/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md

Deployment context:
- Existing Vercel project: goodai_site (team: ktg88, project ID: prj_Bznq9r5slcMCAvtcj7AeUhAebr0n)
- Domain: goodai.au (DNS already pointed to Vercel)
- AI_GATEWAY_API_KEY: server-side only, set in Vercel dashboard
- WEB3FORMS_KEY: client-side form key (not a secret — hardcoded or meta tag)
- Edge runtime considerations: maxDuration = 60 on chat route (Pitfall 5)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production hardening — env security, headers, metadata, bundle optimization</name>
  <files>vercel.json, .env.example, src/app/layout.tsx, src/app/api/chat/route.ts, next.config.ts</files>
  <action>
1. vercel.json — create/update with:
   - Security headers: X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin
   - Cache headers: immutable for /_next/static/*, s-maxage=31536000
   - No rewrites needed (App Router handles routing)

2. .env.example — document required env vars:
   ```
   # Server-side only — set in Vercel dashboard, never prefix with NEXT_PUBLIC_
   AI_GATEWAY_API_KEY=your_ai_gateway_key_here

   # Client-side form key (not a secret)
   NEXT_PUBLIC_WEB3FORMS_KEY=your_web3forms_access_key
   ```

3. src/app/api/chat/route.ts — verify security:
   - Confirm `process.env.AI_GATEWAY_API_KEY` (NOT `process.env.NEXT_PUBLIC_AI_GATEWAY_API_KEY`)
   - Confirm `export const maxDuration = 60` is set (Pitfall 5: Edge timeout)
   - Confirm error responses return friendly messages, not stack traces (DEPL-04)
   - Remove any console.log statements that would fire in production

4. src/app/layout.tsx — production metadata:
   - Add viewport meta: `width=device-width, initial-scale=1, viewport-fit=cover`
   - Add theme-color meta: `#0C0C0C` (matches Deep Tech Onyx)
   - Ensure DM Sans and JetBrains Mono are preloaded via next/font (already should be from Phase 1)
   - Add Open Graph tags: title "Good'ai — Business Automations, Sorted", description targeting Perth SMEs
   - Add `<meta name="robots" content="index, follow" />`

5. next.config.ts — production optimizations:
   - Ensure `output: 'standalone'` is NOT set (Vercel handles this)
   - Verify GLSL raw-loader config is present for both turbopack and webpack (Phase 1 setup)
   - Add `images: { formats: ['image/avif', 'image/webp'] }` if any images exist
   - Suppress known console warnings with `reactStrictMode: true` in production

6. Grep entire src/ for any `console.log`, `console.warn`, `console.error` that should not fire in production. Remove debug logs. Keep only intentional error boundary logs. This directly addresses DEPL-04 (zero console errors).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10 && grep -r "NEXT_PUBLIC_AI_GATEWAY" src/ ; echo "Exit: $?"</automated>
  </verify>
  <done>Build succeeds. No NEXT_PUBLIC_AI_GATEWAY_API_KEY references exist in src/. Security headers configured. All console.log debug statements removed. Metadata complete.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel and run Lighthouse audit</name>
  <files>vercel.json</files>
  <action>
1. Deploy to Vercel production:
   ```bash
   vercel --prod --yes
   ```
   If auth needed, this becomes a checkpoint. Otherwise proceed.

2. After deploy completes, verify the production URL:
   ```bash
   curl -s -o /dev/null -w "%{http_code}" https://goodai.au
   ```
   Expected: 200

3. Check security headers are applied:
   ```bash
   curl -sI https://goodai.au | grep -i "x-content-type\|x-frame\|referrer"
   ```

4. Run Lighthouse CI audit (if `npx @lhci/cli` available, otherwise use Chrome DevTools):
   ```bash
   npx -y lighthouse https://goodai.au --output=json --chrome-flags="--headless --no-sandbox" --throttling-method=simulate --preset=perf | node -e "const r=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log('Performance:',Math.round(r.categories.performance.score*100))"
   ```
   Target: 90+ performance score.

5. Check for console errors on production:
   - Use `npx -y puppeteer-core` or manual Chrome DevTools
   - Open https://goodai.au, check console for errors
   - Verify AI_GATEWAY_API_KEY is NOT visible in browser network tab or source maps

6. If Lighthouse score is below 90, identify bottlenecks:
   - Check if Three.js bundle is too large (should be ~150KB gzipped) — consider dynamic import chunk
   - Check if fonts cause CLS (should be 0 with next/font)
   - Check if shader canvas blocks LCP (should not — it's behind UI)
   - Apply fixes and redeploy
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://goodai.au</automated>
  </verify>
  <done>Site returns 200 at goodai.au. Security headers present. Lighthouse performance >= 90. No console errors. AI_GATEWAY_API_KEY not exposed in client bundle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production deployment at goodai.au with all responsive + performance optimizations from Plans 01 and 02</what-built>
  <how-to-verify>
    1. Open https://goodai.au on desktop browser — verify shader renders, cursor works, chat responds, lead form submits
    2. Open https://goodai.au on your phone — verify no custom cursor, layout is mobile-adapted, shader renders (lower quality is fine), chat works, lead card fields stack vertically
    3. Open Chrome DevTools > Console — verify zero errors
    4. Open Chrome DevTools > Network — verify AI_GATEWAY_API_KEY does not appear in any request visible to the browser
    5. Run Lighthouse audit in Chrome DevTools > Lighthouse tab — verify Performance >= 90
    6. Test on slow 3G throttling — page should be usable within 3 seconds
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `curl https://goodai.au` returns 200
2. Security headers present in response
3. Lighthouse performance score >= 90
4. `grep -r "NEXT_PUBLIC_AI_GATEWAY" src/` returns nothing
5. Browser console on goodai.au shows zero errors
6. Mobile device shows no cursor, correct layout, working chat
</verification>

<success_criteria>
- All 5 DEPL requirements addressed: live on goodai.au (DEPL-01), API key server-only (DEPL-02), sub-2s load (DEPL-03), zero console errors (DEPL-04), Lighthouse 90+ (DEPL-05)
- Human verification confirms desktop and mobile experience
- No secrets exposed in client-side code or network requests
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-deploy/05-02-SUMMARY.md`
</output>
