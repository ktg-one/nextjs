---
description: "Create a pull request from current branch"
---

Create a PR:

1. Run `git log main..HEAD --oneline` to see commits
2. Draft PR title (short, under 70 chars) and description
3. Create PR: `gh pr create --title "..." --body "..."`
4. Report the PR URL
